node_modules/
dist/
dist-test/

# Local Claude Code settings (machine/session-specific)
.claude/settings.local.json

# Local sync state — machine-specific, contains the sync baseline and object backups
.iobroker-sync/

# Downloaded ioBroker type definitions
.iobroker/

*.log

# TLS test certificate, generated on first test run (never committed)
test/fixtures/*.pem

# Personal release checklist — kept locally, never published
docs/GOING-PUBLIC.md
